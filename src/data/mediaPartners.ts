export type MediaPartner = {
  name: string;
  logoSrc: string;
  href?: string;
};

export const mediaPartners: MediaPartner[] = [
  // Add media partners here. Logos should live in /public/media and be referenced as /media/<file-name>.
];
